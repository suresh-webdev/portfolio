// Shared page geometry.
//
// The left gutter is wider than the right because the conduit lives in it —
// the page is deliberately off-centre, which also stops every section from
// starting on the same optical axis as a centred container would.
export const PAD = "px-6 sm:px-8 md:pl-28 md:pr-12 lg:pr-16";
export const SHELL = "w-full max-w-[1440px]";

/** Full-bleed sections opt out of the horizontal padding entirely. */
export const BLEED = "px-0";
